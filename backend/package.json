{
  "name": "nexus-os-backend",
  "version": "2.1.0",
  "description": "NEXUS OS — Home Server Panel Backend",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "dev":     "node --watch server.js",
    "install-deps": "npm install"
  },
  "dependencies": {
    "fastify":             "^4.28.0",
    "@fastify/cors":       "^9.0.1",
    "@fastify/jwt":        "^8.0.1",
    "@fastify/websocket":  "^8.3.1",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/static":     "^7.0.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
